import { createUseStyles } from "react-jss";

export const useStyle = createUseStyles({
  container: {
    margin: "20px",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
  btnContainer: {
    height: "3.2rem",
    alignContent: "center",
  },
});
